export enum SortingValues {
  DateAsc,
  DateDesc,
  TitleAsc,
  TitleDesc,
  RatingAsc,
  RatingDesc,
  RuntimeAsc,
  RuntimeDesc,
}

export enum SortOrder {
  Asc = 'asc',
  Desc = 'desc',
}

export enum SearchBy {
  Title = 'title',
  Genres = 'genres',
}
